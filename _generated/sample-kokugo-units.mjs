import { createServer } from 'vite'; import fs from 'node:fs';
const server=await createServer({server:{middlewareMode:true},appType:'custom',logLevel:'error'});
try{const {SUBJECT_DATA}=await server.ssrLoadModule('/src/data/subjectData.ts');const screen=fs.readFileSync('src/components/ModeSelectionScreen.tsx','utf8');const names={};for(const m of screen.matchAll(/name:\s*'([^']+)'[^\n]*?mode:\s*'((?:KOKUGO_G\d)_[^']+)'/g))names[m[2]]=m[1];
for(const [mode,ps] of Object.entries(SUBJECT_DATA).filter(([k])=>/^KOKUGO_G\d_U\d+$/.test(k))){const qs=[];const seen=new Set();for(const p of ps){const q=p.question.replace(/「[^」]{8,}」/g,'「…」').replace(/\d+/g,'#').replace(/\s+/g,' ');if(!seen.has(q)){seen.add(q);qs.push(p.question.replaceAll('\n',' ').slice(0,180));}if(qs.length>=5)break;}console.log(`${mode}\t${names[mode]||''}\t${qs.join(' || ')}`)}}finally{await server.close()}
