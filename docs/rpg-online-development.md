# 学習ローグ RPGオンライン（開発中）

通常公開用モードではありません。タイトルの入口・起動操作・画面描画をすべて `isDebugModeActive` で制限しています。`VITE_ENABLE_DEBUG_FEATURES=true` のビルドで、さらにアプリ内デバッグモードを有効にしたときだけ起動できます。保存データ経由の起動も不可です。

## 起動

```powershell
pnpm run dev:debug
```

Windowsにも対応した起動スクリプトです。通常の `dev` や本番ビルドの設定は変更しません。

1. 表示されたローカルURLを開く。
2. 初回の学年設定等を終える。
3. タイトル下のバージョン表示を押し、`System Release Notes v1.0.7` を10回押して既存のデバッグモードを有効にする。
4. デバッグ画面から「戻る」でタイトルへ戻る。
5. 「RPGオンライン　開発中・デバッグ限定」を選択する。デイリー課題のお知らせを「あとで」で閉じた場合は、再度入口を選択する。

ゲーム画面にも「開発中」を表示します。アプリを再読み込みすると既存仕様に従ってデバッグ状態は解除されます。

## 現時点で遊べる内容

- オリジナルの16pxドット描画。64×44マスの地形、森、川、道、町、時計塔。シードごとのランダム生成と全拠点への到達保証。
- 最大40人の自由探索、最大4人の任意加入チーム。矢印キー・WASD・タップ経路移動・スマートフォン方向ボタン。
- 算数（たし算・かけ算）、既存問題データを利用した小学5年理科。正解でエナジー3、不正解でも1。
- 既存定義・既存イラストの6種類のカード。えんぴつ攻撃、ノートで防御、ランドセルタックル、上履きキック、定規で叩く、知らんぷり。
- 山札・手札・捨て札、エナジー、ブロック、びくびく、ドロー、カード追加・強化、レベルアップ。
- 町と焚き火で無料回復、30コインで同名カード強化、25コインで購入。宝箱・図書委員イベントで育成。
- 通常の敵は個人戦。3体の試験官と校長はHP・討伐状態を全員で共有。近くの同じチームの仲間と共闘すると攻撃力が上がり、探索中は応援で仲間を回復。
- 試験官3体の撃破で校長の結界解除。校長の初戦時に参加人数に合わせてHPを調整し、撃破で全員勝利。
- 戦闘からの離脱、敗北時の町への復帰、ひとり練習モード。

## 通信

既存プロジェクトのPeerJSを使用しています。ホスト1台が行動を検証し、最大39接続へ250msごとに変更状態を配信します。地形は入室時のみ送信し、以降は参加者・戦闘・拠点の状態を同期します。移動速度、距離、カード所持とコスト、問題の回答済み状態、人数上限をホスト側で検証します。

標準ではPeerJSの標準シグナリング接続を使用します。ローカル検証・独自サーバーは次のVite環境変数で切り替えられます。

```text
VITE_RPG_PEER_HOST=127.0.0.1
VITE_RPG_PEER_PORT=9000
VITE_RPG_PEER_PATH=/rpg
VITE_RPG_PEER_SECURE=false
```

開発用環境変数を本番設定に追加しないでください。

## 検証

```powershell
pnpm run test:rpg
pnpm run test:rpg:access
pnpm run test:production-debug-lock
pnpm run test:title-resume-isolation
node --max-old-space-size=8192 node_modules/typescript/bin/tsc -p tsconfig.rpg.json
pnpm run audit:english:gate
```

40接続のWebRTCブラウザーテストは、別ターミナルでシグナリングサーバーを起動して実行します。

```powershell
npm exec --yes --package=peer@1.0.2 -- peerjs --port 9000 --path /rpg
pnpm run test:rpg:browser
```

80シードの到達性、40人の校長討伐、カード処理、町、チーム上限、二重取得防止、復活、40本の実WebRTC接続、41人目の拒否、切断、PC/スマートフォン表示を検証。画面キャプチャは `tmp/rpg-qa/` に保存します。ブラウザー用の制御入口はテストサーバーだけが生成し、製品側には含みません。

## 開発段階の制限

- ホストが退出・再読み込みすると部屋は終了します。ホスト移譲、再接続、冒険の途中保存は未実装です。メインの冒険のセーブには干渉しません。
- 40接続テストは同一PC・ローカルシグナリング環境での検証です。実際の40台の学校端末、学校回線、別ネットワーク間の接続・負荷は未検証です。NATや学校の通信制限によっては別途TURN対応が必要です。
- メインモードの全カード・遺物・召喚・イベントを移植したものではなく、現在は上記6カードと2つの学習テーマで構成した開発版です。課題の回答実績・ランキングへの連携もまだありません。
- 本番の認証・永続サーバー・不正対策は未導入です。参加者はルームコードを知る開発テスターを想定しています。
- 公開サイトへのデプロイやストア提出は行っていません。

## 主なファイル

- `src/rpg/engine.ts`: マップ・参加者・学習・戦闘・町・チームの状態遷移。
- `src/rpg/network.ts`: PeerJSのホスト権威型ルーム。
- `src/rpg/WorldCanvas.tsx`: ドット描画・マップクリック座標。
- `src/rpg/RpgOnline.tsx` / `rpg.css`: ロビー・探索・戦闘・町・チーム編成。
- `src/App.tsx`: デバッグ限定のタイトル入口・画面ガード。
- `src/services/storageService.ts`: メインセーブからの除外。
- `src/data/rpgEnglish.ts`: 既存翻訳処理に追加した画面コピー。
